# Sanity Studio configuration.
# Copy this file to .env before running `npm run dev` or `npm run deploy`.
#
# These must match the values in app/.env.example — the Studio writes to the same
# project and dataset the website reads from.
SANITY_STUDIO_PROJECT_ID=kavbcyru
SANITY_STUDIO_DATASET=production
